<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.project.mappers.UserDAO">
	<select id="selectView" resultType="com.spring.project.vo.UserVO">
		select 
			userid,
			pwd,
			name,
			sex,
			phone,
			address,
			loginDateTime,
			regiDate
		from member
		where userid = #{userid}
	</select>
	
	
	<select id="listUsers" resultType="com.spring.project.vo.UserVO">
		select 
			userid,
			pwd,
			name,
			sex,
			phone,
			address,
			loginDateTime,
			regiDate,
			loginCheck
		from member
	</select>
</mapper>